import { ValidationCustomPipe } from './validation-custom.pipe';

describe('ValidationCustomPipe', () => {
  it('should be defined', () => {
    expect(new ValidationCustomPipe()).toBeDefined();
  });
});
